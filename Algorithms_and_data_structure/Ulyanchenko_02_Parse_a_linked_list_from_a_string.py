class Node:
    def __init__(self):
        self.data = 0
        self.next = 
    def parse(elements):
        if elements[0] == "NULL":
            return None  # Базовий випадок
        else:
            Node9
    # Тут ми створюємо об'єкт Node
    # Яким буде цей рядок, якщо ми беремо перше число
    # і викликаємо parse(elements[1:]) для наступного вузла?
